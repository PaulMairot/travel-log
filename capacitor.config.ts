import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.travellog.firstrespondant',
  appName: 'comem-travel-log-ionic-starter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
